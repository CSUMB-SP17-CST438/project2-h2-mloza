test:
    override:
        - python -m tests/unit_tests
        - python -m tests/socketio_tests
        python -m tests/integration_tests
        
# database:
#   override:
#     - mysql -u ubuntu circle_test < my-database-setup.sql
         
deployment:
    production:
        branch: master
        heroku:
            appname: rocky-waters-83090
